<h1> All Articles</h1>
<ul id="artilcles">
  <% @articles.each do |article| %>
  <li>
    <%= link_to article.title, article_path(article), id: "article_#{article.id}" %>
  </li>
    <% end %>

</ul>
<%= link_to :"Create new Articles", new_article_path, class: "new_article" %>
